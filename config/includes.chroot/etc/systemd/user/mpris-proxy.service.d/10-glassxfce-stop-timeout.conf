[Service]
# BlueZ's optional MPRIS media-control bridge has been observed to hold the
# user manager open for the full systemd stop timeout on some live hardware.
# Keep Bluetooth itself enabled, but force this helper to terminate promptly.
TimeoutStopSec=5s
KillMode=mixed
SendSIGKILL=yes
