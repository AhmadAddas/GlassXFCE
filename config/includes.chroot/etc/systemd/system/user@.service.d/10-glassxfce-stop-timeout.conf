[Service]
# Do not let a single misbehaving per-user helper stall live-media shutdown
# for the default 90 seconds. This remains long enough for normal session
# cleanup while bounding shutdown latency on removable-media boots.
TimeoutStopSec=20s
