# GlassXFCE compatibility theme shim.
# live-build-generated installer/menu fragments may source /boot/grub/theme.cfg.
# Keep that path valid without loading the stock opaque GRUB theme container.
set theme=
insmod png
if [ -f /boot/grub/splash.png ]; then
    background_image /boot/grub/splash.png
fi
