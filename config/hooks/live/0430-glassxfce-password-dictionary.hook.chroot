#!/bin/sh
set -eu

# Calamares/libpwquality can report "error loading dictionary" when CrackLib's
# recommended word list is omitted by --apt-recommends false. Build the cache
# explicitly while the live root is being assembled.
if command -v update-cracklib >/dev/null 2>&1; then
  update-cracklib
fi
