# GlassXFCE Live

A Debian 13 XFCE live ISO project focused on a lightweight, modern, macOS-inspired desktop.

## Commit style

This project uses lower-case Conventional Commit messages, for example:

```text
feat: add debian live build base
fix: correct picom startup
ci: add tagged iso release
chore: update repository metadata
```
